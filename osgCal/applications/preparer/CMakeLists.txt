SET(TARGET_NAME osgCalPreparer)

SET(OSG_LIBS osgViewer osgDB osgText osg osgUtil osgGA OpenThreads)

SET(SOURCE_FILES osgCalPreparer.cpp)

INCLUDE_DIRECTORIES(
  ${OSGCAL_INCLUDE_DIR}
  ${OSG_INCLUDE_DIR}
  ${CAL3D_INCLUDE_DIR}
  ${OPENTHREADS_INCLUDE_DIR}
)

LINK_DIRECTORIES(
  ${OPENTHREADS_LIBRARY_DIR}
  ${OSG_LIBRARY_DIR}
  ${CAL3D_LIBRARY_DIR}
)

OSGCAL_APPLICATION( ${TARGET_NAME} ${SOURCE_FILES} )

LINK_INTERNAL(${TARGET_NAME} osgCal ${OSG_LIBS})

